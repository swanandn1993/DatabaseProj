create database CPApp;
use CPApp;


<--create table users(patientid int IDENTITY(1,1), lname varchar(30), fname varchar(30), dob varchar(10), gender varchar(20), ethnicity varchar(20),PRIMARY KEY(patientid) );

<--create table questions(questionid int IDENTITY(1,1), qname varchar(150), primary key(questionid));

<--create table episode(patientid int, episodeid int identity(1,1), questionid int, answer varchar(200), primary key(patientid, episodeid, questionid));






